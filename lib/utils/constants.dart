import 'dart:ui';

///String used to generate random hexadecimal values
const kColorString = 'A1B2C3D4E5F67890';

/// This is the length of the hexadecimal color
const kColorLength = 6;

/// The prefix of the generated color
const kColorPrefix = '0xff';

/// default color
const kColorDefault = Color(0xffFFFFFF);

/// animation duration (500 milliseconds)
const kAnimationDuration = Duration(milliseconds: 500);
